using UnityEngine;

public class AnimationFocusCamera : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    Animation animation;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
